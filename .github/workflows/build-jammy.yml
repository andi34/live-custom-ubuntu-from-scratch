name: build-jammy

on:
  push:
    branches:
      - master
      - photobooth-docker
  pull_request:
    branches:
      - master
      - photobooth-docker
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    if: github.repository == 'andi34/live-custom-ubuntu-from-scratch'

    steps:
      - uses: actions/checkout@v2

      - name: Build
        run: |
          cd scripts
          sed -i -E 's/TARGET_UBUNTU_VERSION=(.*)/TARGET_UBUNTU_VERSION="jammy"/g' config.sh
          ./build.sh -
          cd ..
      - name: Archive artifacts
        uses: actions/upload-artifact@v3
        with:
          name: photobooth-jammy
          path: |
            scripts/photobooth.iso
            scripts/image/md5sum.txt
